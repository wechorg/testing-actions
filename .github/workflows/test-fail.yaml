name: Failure Test
on:
  workflow_dispatch:

jobs:
  success-job:
    name: Job will succeeed
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Failed step
        shell: bash
        run: |
          false

      - name: Printout Step
        if: failure()
        uses: ./.github/actions/dummy-action
